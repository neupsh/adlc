name: _validate (throwaway — delete before merge)

on:
  push:
    branches: [fix/job-env-secret-startup]

jobs:
  control:
    runs-on: ubuntu-latest
    steps:
      - run: echo "control job parsed and ran — _validate harness is OK"
